---
phase: 01-guided-first-run-tour
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/CurrencyRow.tsx
  - src/components/SearchBar.tsx
  - src/components/CurrencyListModal.tsx
  - src/components/ThemeToggle.tsx
  - src/components/InstallButton.tsx
  - src/app/page.tsx
autonomous: true
requirements: [TOUR-06]
must_haves:
  truths:
    - "All 8 tour anchors carry a dedicated data-tour attribute on a real, natively-focusable DOM element"
    - "The base-row anchor (tour-base-row) follows whichever row is currently base via the isBase conditional, so it is valid regardless of display order or virtualization"
    - "The tour-install anchor is always present in the DOM even when the PWA install prompt is unavailable (InstallButton no longer early-returns null)"
    - "Every data-tour attribute stacks alongside the element's existing aria-label/title without removing them"
    - "No anchor selects on a translated aria-label — data-tour is the only tour-owned selector"
  artifacts:
    - path: "src/components/CurrencyRow.tsx"
      provides: "tour-base-row (conditional on isBase) on the row wrapper + tour-amount-input on the base-row input"
      contains: "data-tour"
    - path: "src/components/SearchBar.tsx"
      provides: "tour-search on the search input"
      contains: "tour-search"
    - path: "src/components/CurrencyListModal.tsx"
      provides: "tour-list-settings on the list/settings trigger button"
      contains: "tour-list-settings"
    - path: "src/components/ThemeToggle.tsx"
      provides: "tour-theme-toggle on the toggle button"
      contains: "tour-theme-toggle"
    - path: "src/components/InstallButton.tsx"
      provides: "always-rendered tour-install wrapper div (early null-return removed)"
      contains: "tour-install"
    - path: "src/app/page.tsx"
      provides: "tour-share on the share button + tour-historical-date on the date input"
      contains: "tour-share"
  key_links:
    - from: "src/lib/tourSteps.ts"
      to: "the live DOM"
      via: "each DriveStep.element CSS selector resolves to an element added in this plan"
      pattern: "data-tour=\"tour-"
    - from: "src/components/CurrencyRow.tsx"
      to: "isBase state"
      via: "data-tour={isBase ? 'tour-base-row' : undefined} on the row wrapper"
      pattern: "isBase \\? 'tour-base-row'"
---

<objective>
Add the 8 `data-tour="..."` anchor attributes to the real DOM elements at the exact locations enumerated in 01-UI-SPEC.md, using the exact selector names that 01-01's `buildTourSteps` already emits. This completes TOUR-06 (01-01 defined the selectors; this plan makes them real on the page). Also restructure `InstallButton` so its anchor wrapper always renders (removing the `if (!deferred) return null` early-return) so step 8's anchor never 404s.

Purpose: Make every step's `element` selector resolvable so Plan 03's `driver()` can spotlight each anchor. Without these attributes the tour would silently skip every step.
Output: 6 files modified with additive `data-tour` attributes (5 anchors) + one component restructure (InstallButton always-render wrapper); zero behavioral or styling changes to the app itself.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-guided-first-run-tour/01-CONTEXT.md
@.planning/phases/01-guided-first-run-tour/01-UI-SPEC.md
@.planning/phases/01-guided-first-run-tour/01-PATTERNS.md

<interfaces>
<!-- Contracts from Plan 01 (already written, do not modify). These are the exact selector -->
<!-- strings buildTourSteps() emits; the attributes added here MUST match them character-for-character. -->

The 8 `data-tour` values that `src/lib/tourSteps.ts` (Plan 01) emits as `element: '[data-tour="..."]'`, in order:
  1. tour-base-row        (CurrencyRow.tsx — row wrapper, conditional on isBase)
  2. tour-amount-input    (CurrencyRow.tsx — base-row <input>)
  3. tour-search          (SearchBar.tsx — search <input>)
  4. tour-list-settings   (CurrencyListModal.tsx — trigger <button>, line ~248)
  5. tour-share           (page.tsx — share <button>, line ~285)
  6. tour-theme-toggle    (ThemeToggle.tsx — <button>, lines ~18-23)
  7. tour-historical-date (page.tsx — date <input>, line ~309)
  8. tour-install         (InstallButton.tsx — always-present wrapper <div>)

Reserved / forbidden names (Phase 2/3 own these — do NOT use): tour-replay, tour-help.

Exact current line targets (verified against the working tree):
  - CurrencyRow.tsx:73  → `<div id='currencyItem' style={style}>` (row wrapper)
  - CurrencyRow.tsx:87-98 → base-row `<input type="text">` (rendered when isBase, alongside aria-label at :96)
  - SearchBar.tsx:87-98 → search `<input>` (alongside aria-label="Search currencies to add" at :94)
  - CurrencyListModal.tsx:248 → `<button type="button" onClick={openModal} aria-label="Open currency list and settings">`
  - ThemeToggle.tsx:18-23 → `<button ... aria-label="Toggle light/dark theme" ...>`
  - page.tsx:285-296 → share `<button onClick={onShare} ... aria-label="Copy shareable link">`
  - page.tsx:309-316 → `<input type="date" ... aria-label="View rates as of a past date">`
  - InstallButton.tsx:30 → `if (!deferred) return null;` (to be removed); wrapper at :33 `<div className="w-full flex justify-center my-2">`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the two CurrencyRow anchors (tour-base-row conditional, tour-amount-input)</name>
  <read_first>
    - src/components/CurrencyRow.tsx (full, 143 lines — `isBase` computed at :42; row wrapper `<div id='currencyItem' style={style}>` at :73; base-row `<input>` with existing aria-label at :87-98)
    - .planning/phases/01-guided-first-run-tour/01-UI-SPEC.md (Anchor Map rows 1-2 + Edge Cases "Virtualized list" and "First-time visitor whose default base currency row happens to be the very first row rendered")
    - .planning/phases/01-guided-first-run-tour/01-PATTERNS.md (section "src/components/CurrencyRow.tsx")
  </read_first>
  <action>
    In src/components/CurrencyRow.tsx add exactly two attributes; change nothing else (no logic, no imports — `isBase` already exists at line 42).

    On the row wrapper `<div id='currencyItem' style={style}>` (line 73), add a conditional attribute `data-tour={isBase ? 'tour-base-row' : undefined}` so the `tour-base-row` anchor is present ONLY on whichever row is currently the base (per UI-SPEC Anchor Map row 1 and the Edge Case rationale: anchoring by `isBase` rather than by array position keeps the selector valid regardless of sort order or react-window virtualization, since the base row is the one being spotlighted). Using `undefined` (not `false` or `''`) means React omits the attribute entirely on non-base rows, so `document.querySelector('[data-tour="tour-base-row"]')` resolves to exactly one element.

    On the base-row `<input type="text">` (lines 87-98, the `isBase` branch), add `data-tour="tour-amount-input"` alongside the existing `aria-label={...}` at line 96 — do NOT remove or alter the aria-label. This input always renders for whichever row `isBase` is true, so the anchor is stable even though the row it attaches to changes.
  </action>
  <verify>
    <automated>grep -q "isBase ? 'tour-base-row'" src/components/CurrencyRow.tsx && grep -q 'data-tour="tour-amount-input"' src/components/CurrencyRow.tsx && grep -q 'aria-label={`${cur.toUpperCase()} amount' src/components/CurrencyRow.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "data-tour={isBase ? 'tour-base-row' : undefined}" src/components/CurrencyRow.tsx` returns 1 (conditional attribute on the row wrapper, value `undefined` on non-base rows).
    - `grep -c 'data-tour="tour-amount-input"' src/components/CurrencyRow.tsx` returns 1 (on the base-row input).
    - The existing base-row input `aria-label` line is unchanged (source assertion: `grep -q 'amount (supports math' src/components/CurrencyRow.tsx` still succeeds).
    - No new import and no change to the `isBase`/`onSelectBase`/render logic (only the two attributes are added).
  </acceptance_criteria>
  <done>CurrencyRow carries tour-base-row (conditional on isBase, one element in the DOM at a time) and tour-amount-input, both stacked alongside existing attributes with no behavioral change.</done>
</task>

<task type="auto">
  <name>Task 2: Add tour-search, tour-list-settings, tour-theme-toggle, and the two page.tsx anchors (tour-share, tour-historical-date)</name>
  <read_first>
    - src/components/SearchBar.tsx (lines 84-106 — search `<input>` with role="combobox" + aria-label at :93-97)
    - src/components/CurrencyListModal.tsx (lines 246-251 — trigger `<button onClick={openModal} aria-label="Open currency list and settings">` at :248)
    - src/components/ThemeToggle.tsx (full, 28 lines — the single `<button>` at :18-23)
    - src/app/page.tsx (lines 285-296 share button; lines 309-316 date input — both already carry aria-label/title)
    - .planning/phases/01-guided-first-run-tour/01-UI-SPEC.md (Anchor Map rows 3, 4, 5, 6, 7)
    - .planning/phases/01-guided-first-run-tour/01-PATTERNS.md (sections "SearchBar.tsx", "CurrencyListModal.tsx", "ThemeToggle.tsx", "src/app/page.tsx" data-tour additions)
  </read_first>
  <action>
    Add one `data-tour` attribute to each of five elements, each stacked alongside (never replacing) the existing `aria-label`/`title`/`role` attributes. Change nothing else in these files.

    src/components/SearchBar.tsx: on the search `<input>` (lines 87-98), add `data-tour="tour-search"` alongside the existing `aria-label="Search currencies to add"` (keep `role="combobox"`, `aria-expanded`, `aria-controls` intact).

    src/components/CurrencyListModal.tsx: on the trigger `<button type="button" onClick={openModal} aria-label="Open currency list and settings">` (line 248), add `data-tour="tour-list-settings"`. Do NOT touch the `<dialog id="currency_list_modal">` below it — the modal must stay closed during the tour (Plan 03 / UI-SPEC Interaction Timing step-4 constraint); this plan only anchors the trigger button.

    src/components/ThemeToggle.tsx: on the `<button>` (lines 18-23), add `data-tour="tour-theme-toggle"` alongside the existing `title`/`aria-label="Toggle light/dark theme"`.

    src/app/page.tsx: on the share `<button onClick={onShare} ...>` (lines 285-296), add `data-tour="tour-share"` alongside the existing `title`/`aria-label="Copy shareable link"`. On the historical date `<input type="date" ...>` (lines 309-316), add `data-tour="tour-historical-date"` alongside the existing `aria-label="View rates as of a past date"`. Add NO tour-start logic or imports in this task — the tour wiring is Plan 03; this task only adds the two attributes.
  </action>
  <verify>
    <automated>grep -q 'data-tour="tour-search"' src/components/SearchBar.tsx && grep -q 'data-tour="tour-list-settings"' src/components/CurrencyListModal.tsx && grep -q 'data-tour="tour-theme-toggle"' src/components/ThemeToggle.tsx && grep -q 'data-tour="tour-share"' src/app/page.tsx && grep -q 'data-tour="tour-historical-date"' src/app/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-tour="tour-search"' src/components/SearchBar.tsx` == 1; the existing `aria-label="Search currencies to add"` still present.
    - `grep -c 'data-tour="tour-list-settings"' src/components/CurrencyListModal.tsx` == 1; on the `onClick={openModal}` button; the `<dialog>` element unchanged.
    - `grep -c 'data-tour="tour-theme-toggle"' src/components/ThemeToggle.tsx` == 1; existing aria-label/title retained.
    - `grep -c 'data-tour="tour-share"' src/app/page.tsx` == 1 AND `grep -c 'data-tour="tour-historical-date"' src/app/page.tsx` == 1; both existing aria-labels retained; no `driver`/`buildTourSteps` import added in this task.
  </acceptance_criteria>
  <done>All five remaining always-present anchors carry their data-tour attribute stacked on the existing accessible elements, with no behavioral or dialog-state changes.</done>
</task>

<task type="auto">
  <name>Task 3: Restructure InstallButton so the tour-install wrapper always renders</name>
  <read_first>
    - src/components/InstallButton.tsx (full, 47 lines — `if (!deferred) return null;` at :30; wrapper `<div className="w-full flex justify-center my-2">` at :33; the beforeinstallprompt/appinstalled effect at :15-28 must stay unchanged)
    - src/app/page.tsx (line 348-350 — `<footer>` renders `<InstallButton />`; confirms the anchor lands in the footer)
    - .planning/phases/01-guided-first-run-tour/01-UI-SPEC.md (Anchor Map row 8 + Edge Case "PWA install anchor may have no visible button")
    - .planning/phases/01-guided-first-run-tour/01-PATTERNS.md (section "src/components/InstallButton.tsx" — before/after refactor with `min-h-0`)
    - .planning/phases/01-guided-first-run-tour/01-RESEARCH.md (Code Examples: "InstallButton always-present anchor wrapper")
  </read_first>
  <action>
    In src/components/InstallButton.tsx remove the `if (!deferred) return null;` early-return line (line 30) so the component ALWAYS renders its wrapper `<div>`. Add `data-tour="tour-install"` to that wrapper `<div>` and add `min-h-0` to its className (final className: `w-full flex justify-center my-2 min-h-0`, per UI-SPEC/PATTERNS so an empty container does not reserve unwanted height). Make the inner `<button>` conditional on `deferred` by wrapping it in `{deferred && ( ... )}` — the button and its `onClick` install logic (`deferred.prompt()` → `deferred.userChoice` → `setDeferred(null)`) are otherwise unchanged. Leave the `beforeinstallprompt`/`appinstalled` `useEffect` (lines 15-28) and the `BeforeInstallPromptEvent` interface completely unchanged.

    Result: `[data-tour="tour-install"]` always resolves to the wrapper div (so step 8 never 404s), while the visible install button still only appears when the browser fired `beforeinstallprompt`. The step-8 fallback COPY (shown when the button is absent) already lives in `TOUR_INSTALL_FALLBACK_DESCRIPTION` in tourSteps.ts (Plan 01) and is swapped in by Plan 03 — do NOT add fallback copy in this component.
  </action>
  <verify>
    <automated>grep -q 'data-tour="tour-install"' src/components/InstallButton.tsx && grep -q 'min-h-0' src/components/InstallButton.tsx && ! grep -q 'if (!deferred) return null' src/components/InstallButton.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-tour="tour-install"' src/components/InstallButton.tsx` == 1, on the wrapper `<div>`.
    - The early `if (!deferred) return null;` is gone (`grep -c 'if (!deferred) return null' src/components/InstallButton.tsx` == 0).
    - The wrapper className includes `min-h-0`; the inner button is conditional (`grep -q '{deferred &&' src/components/InstallButton.tsx`).
    - The `beforeinstallprompt`/`appinstalled` effect is unchanged (source assertion: `grep -q "addEventListener('beforeinstallprompt'" src/components/InstallButton.tsx` still succeeds).
    - `npm run build` succeeds (component compiles; TypeScript happy with the restructured render).
  </acceptance_criteria>
  <done>InstallButton always renders a `data-tour="tour-install"` wrapper (button still conditional on the install prompt), so step 8's anchor is guaranteed present; build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React render → DOM | `data-tour` attribute values are rendered into the live DOM and later matched by `document.querySelector` in Plan 03 |
| DOM structure → tour selector engine | driver.js queries these anchors by CSS selector; a wrong/absent attribute silently breaks a step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering / Spoofing | `data-tour` attribute values as CSS selectors | mitigate | All `data-tour` values are static string literals authored in this plan (no interpolation of user/URL input into a selector), so there is no attacker-controlled selector-injection path. Values match Plan 01's `buildTourSteps` literals exactly, verified by grep. |
| T-01-02-02 | Denial of Service (silent step drop) | Missing/misnamed anchor → step silently skipped | mitigate | Grep gates assert each of the 8 selectors is present with the exact spelling. `tour-base-row` uses the `isBase` conditional so exactly one element carries it (no duplicate-match ambiguity); `tour-install` wrapper always renders (early-return removed) so step 8 is never dropped. |
| T-01-02-03 | Information Disclosure | New attributes exposing app internals | accept | `data-tour` values are non-sensitive UI-anchor identifiers (e.g. `tour-share`); they expose nothing beyond which controls exist, which is already visible in the UI. No PII, no secrets — client-only cosmetic feature. |
| T-01-02-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs no packages. driver.js was already pinned and audited [OK] in Plan 01 (RESEARCH.md Package Legitimacy Audit); no `[ASSUMED]`/`[SUS]` packages, so no blocking human checkpoint required. |
</threat_model>

<verification>
- `npm run build` succeeds with all 6 modified files compiling.
- `grep -rho 'data-tour="tour-[a-z-]*"' src/components src/app | sort -u` lists the 7 static anchors (tour-amount-input, tour-historical-date, tour-install, tour-list-settings, tour-search, tour-share, tour-theme-toggle) AND `grep -q "isBase ? 'tour-base-row'" src/components/CurrencyRow.tsx` confirms the 8th (conditional) anchor — 8 total, matching the 8 selectors emitted by `src/lib/tourSteps.ts` (Plan 01).
- No existing `aria-label`/`title` was removed (spot-check each modified element retains its accessible name).
- `npx vitest run` still passes (this plan adds no tests and breaks none — Plan 01's fns/tourSteps tests remain green).
</verification>

<success_criteria>
- All 8 tour anchors exist on real, focusable DOM elements with the exact selector names Plan 01 emits; `tour-base-row` follows `isBase`; `tour-install` always renders.
- Every attribute stacks alongside existing accessibility attributes; no behavior, styling, or dialog state changed.
- `npm run build` passes; TOUR-06 is now fully satisfied (selectors defined in Plan 01 + attributes live on the DOM here).
</success_criteria>

<output>
Create `.planning/phases/01-guided-first-run-tour/01-02-SUMMARY.md` when done.
</output>
