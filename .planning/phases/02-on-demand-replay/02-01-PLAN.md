---
phase: 02-on-demand-replay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/svgs.tsx
  - src/app/page.tsx
autonomous: true
requirements: [TOUR-03]
must_haves:
  truths:
    - "A persistent \"?\" help control is visible in the home page's top action row regardless of tourSeen state (renders whether tourSeen is true or false)"
    - "Clicking the \"?\" launches the same Phase-1 tour from step 1 (the centered welcome card first), with full forward/back/skip behavior intact — identical to first-run"
    - "Replaying does not reset or corrupt the seen-flag semantics: the \"?\" onClick never writes tourSeenAtom; tourSeen stays true after it was set, and the auto-start effect never re-fires once tourSeen is true"
    - "The tour build+configure+drive logic lives in ONE reusable startTour() called by BOTH the auto-start effect and the \"?\" onClick — no duplicated driver() config"
    - "Before starting, startTour() destroys any existing tourDriverRef.current instance so a replay never stacks two overlays / leaks the previous driver"
    - "The auto-start path keeps its tourStartedRef + (hydrated && !tourSeen && effectiveAll) gate; the \"?\" path bypasses those gates but routes through the same startTour()"
    - "The mount-only unmount cleanup that destroys tourDriverRef.current is preserved; NO dependency-change teardown is reintroduced (the Phase-1 bug fixed in commit a818626 must not regress)"
    - "The reused driver config is byte-for-byte the Phase-1 config: disableActiveInteraction, allowClose, overlayClickBehavior 'close', Next-button linear (nextBtnText/prevBtnText), doneBtnText, theme-aware overlayColor, missing-anchor filter, and install-fallback description swap"
    - "D-05: the \"?\" is a real <button> with an English aria-label + title; it is NOT a tour step (no data-tour anchor); no new i18n translation keys are added"
  artifacts:
    - path: "src/lib/svgs.tsx"
      provides: "QuestionSvg inline-SVG export (question-mark-circle glyph, mirrors ShareSvg's signature/className/props-spread)"
      contains: "export const QuestionSvg"
    - path: "src/app/page.tsx"
      provides: "reusable startTour() (extracted from the auto-start effect), the auto-start effect rewired to call startTour(), the \"?\" <button onClick={startTour}> added to the top action row, and the preserved mount-only cleanup"
      contains: "startTour"
  key_links:
    - from: "src/app/page.tsx \"?\" button onClick"
      to: "src/app/page.tsx startTour()"
      via: "onClick={() => startTour()} (or onClick={startTour}) on the top-row <button>"
      pattern: "onClick=\\{.*startTour"
    - from: "src/app/page.tsx auto-start useEffect"
      to: "src/app/page.tsx startTour()"
      via: "the gated effect calls startTour() after its hydrated && !tourSeen && effectiveAll + tourStartedRef guard passes"
      pattern: "startTour\\("
    - from: "src/app/page.tsx startTour()"
      to: "src/app/page.tsx tourDriverRef"
      via: "tourDriverRef.current?.destroy() at the top of startTour() before driver(), then tourDriverRef.current = driverObj"
      pattern: "tourDriverRef.current"
    - from: "src/app/page.tsx \"?\" button"
      to: "src/lib/svgs.tsx QuestionSvg"
      via: "import { QuestionSvg, ShareSvg } from '@/lib/svgs' and <QuestionSvg /> rendered inside the button"
      pattern: "QuestionSvg"
---

<objective>
Add a persistent "?" help control to the moneyrate home page (`/`) that relaunches the identical Phase-1 guided tour from step 1, at any time, regardless of `tourSeen` state (TOUR-03). This is one thin vertical slice: after this plan, a real user who already dismissed or completed the tour can click "?" and immediately walk through the full 8-step tour again.

The work is (1) a new `QuestionSvg` icon and (2) a small refactor of `src/app/page.tsx` — extracting the inline tour build+configure+drive logic from the auto-start `useEffect` into ONE reusable `startTour()` that BOTH the auto-start effect and the new "?" `onClick` call. No new tour content, no new steps, no new dependency, no backend.

Purpose: A visitor can reopen the tour whenever they want, without the seen-flag ever blocking them, and without corrupting the once-per-browser auto-start semantics from Phase 1 (TOUR-02 preserved).
Output: `src/lib/svgs.tsx` gains `QuestionSvg`; `src/app/page.tsx` gains `startTour()`, the rewired auto-start effect, and the "?" button; `02-01-SUMMARY.md`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

## Phase Goal

**As a** visitor who already dismissed or completed the moneyrate tour (or just wants a refresher), **I want to** relaunch the identical guided walkthrough at any time via a persistent "?" help control, **so that** I can re-learn the app's key interactions without clearing my browser state or waiting for a first-run trigger that will never fire again.

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-on-demand-replay/02-CONTEXT.md
@.planning/phases/01-guided-first-run-tour/01-UI-SPEC.md
@.planning/phases/01-guided-first-run-tour/01-VERIFICATION.md

<interfaces>
<!-- Contracts already shipped by Phase 1. Do NOT re-derive or rebuild — reuse directly. -->

From src/lib/atoms.ts (Phase 1):
  export const tourSeenAtom = atomWithStorage<boolean>('tourSeen', false); // localStorage key 'tourSeen'
  // D-03: the "?" path MUST NOT write this atom. Only the driver callbacks (already present) set it.

From src/lib/fns.ts (Phase 1) — pure, takes navLangs as a param (does NOT read navigator itself):
  export const resolveTourLocale = (navLangs, supported, fallback?) => Language;

From src/lib/tourSteps.ts (Phase 1):
  export const TOUR_STEP_COUNT = 8;
  export const SUPPORTED_LOCALES: Language[];             // 30 locales, derived from Object.keys(translations)
  export const TOUR_INSTALL_FALLBACK_DESCRIPTION: string; // step-8 copy when no install prompt is captured
  export const buildTourSteps = (locale: Language): DriveStep[]; // [welcome (no element), ...8 anchored feature steps]
  // The last feature step's element is '[data-tour="tour-install"]'.

From driver.js (Phase 1 pinned ^1.6.0):
  import { driver, type Driver } from 'driver.js';   // driver.css imported once in layout.tsx — do NOT import CSS here
  driver(config) -> Driver;  Driver has .drive() and .destroy().

From src/lib/svgs.tsx (the file being modified) — the icon-component pattern to mirror:
  export const ShareSvg = ({ ...props }) => <svg xmlns=... viewBox="0 0 24 24" strokeWidth={1.5}
    stroke="currentColor" className="size-6" {...props} ><path .../></svg>;
  // NOTE: a `QuestionMarkSvg` (question-mark-circle) glyph already exists in this file (lines ~42-44).
  //   Reuse its path `d` for the new `QuestionSvg` export. D-02/downstream require the export be named
  //   `QuestionSvg` (grep gate: `grep -q QuestionSvg src/lib/svgs.tsx`). Do NOT rename or remove
  //   `QuestionMarkSvg`; add `QuestionSvg` alongside it.

From src/app/page.tsx (the file being modified) — the exact existing seams to wire into:
  Line 1:            "use client";  (already a client component)
  Line 24:           import { ShareSvg } from '@/lib/svgs';   -> add QuestionSvg to this named import
  Lines 88-90:       const tourStartedRef = useRef(false); const tourDriverRef = useRef<Driver | null>(null);
  Line 100:          const [tourSeen, setTourSeen] = useAtom(tourSeenAtom);
  Lines 157-160:     const effectiveAll = useMemo(...)  — truthy once real data (or the localStorage stale fallback) resolved
  Lines 165-225:     the auto-start useEffect — the build+configure+drive body to EXTRACT into startTour()
                       (locale resolution, buildTourSteps, missing-anchor filter, install-fallback swap,
                        the full driver({...}) config, tourDriverRef.current = driverObj, driverObj.drive())
  Line 225:          effect dependency array: [hydrated, tourSeen, effectiveAll]
  Line 228:          useEffect(() => () => { tourDriverRef.current?.destroy(); tourDriverRef.current = null; }, []);
                       — the MOUNT-ONLY cleanup. KEEP it exactly. Do NOT add a dep-change teardown (commit a818626 bug).
  Line 361:          <span className='flex gap-2 w-full items-start'>  — the top action row (CurrencyListModal,
                       share <button>, ThemeToggle, SearchBar). The "?" button goes in this span.
  Lines 363-375:     the share <button ... className="h-[52px] w-[30px] shrink-0 flex items-center justify-center relative">
                       — the closest structural analog for the new "?" button (type/onClick/title/aria-label/sizing).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the QuestionSvg icon to src/lib/svgs.tsx</name>
  <read_first>
    - src/lib/svgs.tsx (full — the `ShareSvg` export at lines 5-7 is the signature/className/props-spread pattern to mirror; the existing `QuestionMarkSvg` at lines ~42-44 whose question-mark-circle `d` path you will reuse under the new `QuestionSvg` name)
    - .planning/phases/02-on-demand-replay/02-CONTEXT.md (D-02: question-mark-circle icon, add a new inline SVG component named `QuestionSvg` following the `ShareSvg` pattern; "Claude's Discretion: exact SVG path for the question-mark-circle glyph")
  </read_first>
  <action>
    Modify `src/lib/svgs.tsx` only. Add ONE new named export `QuestionSvg` following the exact `ShareSvg` component shape: an arrow function `({ ...props }) => <svg ...>...</svg>` with `xmlns="http://www.w3.org/2000/svg"`, `fill="none"`, `viewBox="0 0 24 24"`, `strokeWidth={1.5}`, `stroke="currentColor"`, `className="size-6"`, `{...props}` spread on the `<svg>`, and a single `<path>` with `strokeLinecap="round" strokeLinejoin="round"` and a question-mark-circle `d`.

    Per D-02, the glyph is a question-mark-circle. Reuse the `d` path already present on the existing `QuestionMarkSvg` export in this same file (the question-mark-in-a-circle Heroicons outline path) so the visual matches the app's icon set — copy that `d` value into the new `QuestionSvg`. Do NOT rename, remove, or repurpose `QuestionMarkSvg`; the new export MUST be named `QuestionSvg` (D-02 + the downstream grep gate depend on that name). Place `QuestionSvg` as a normal named export among the others (grouped near `ShareSvg` or the other circle-icon exports — placement is cosmetic).

    Add NO fenced code blocks in this plan; follow the identifier `QuestionSvg` and the `ShareSvg` shape above. This file is a pure icon module (named exports only, no default export, no `"use client"` — do not add one).
  </action>
  <verify>
    <automated>grep -q 'export const QuestionSvg' src/lib/svgs.tsx && grep -q 'export const QuestionMarkSvg' src/lib/svgs.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'export const QuestionSvg' src/lib/svgs.tsx` == 1 (the new export exists under the exact name).
    - Source: `grep -c 'export const QuestionMarkSvg' src/lib/svgs.tsx` == 1 (the pre-existing export is preserved, not renamed away).
    - Source (mirrors ShareSvg): the `QuestionSvg` definition contains `viewBox="0 0 24 24"`, `stroke="currentColor"`, `className="size-6"`, and `{...props}` (spot-check the export region — same signature as `ShareSvg`).
    - Source (single-glyph): `QuestionSvg` renders exactly one `<path ... strokeLinecap="round" strokeLinejoin="round" ... />` with a non-empty `d` attribute.
    - Behavior/test-command: `npm run build` succeeds (the module still compiles; TypeScript accepts the new export).
  </acceptance_criteria>
  <done>src/lib/svgs.tsx exports a new `QuestionSvg` question-mark-circle icon mirroring `ShareSvg`, the existing `QuestionMarkSvg` is untouched, and the build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract startTour(), rewire the auto-start effect, and add the "?" replay button to the top action row</name>
  <read_first>
    - src/app/page.tsx (full, 431 lines — CRITICAL regions: the import from '@/lib/svgs' at line 24; the refs `tourStartedRef`/`tourDriverRef` at lines 88-90; `tourSeen`/`setTourSeen` at line 100; the `effectiveAll` memo at 157-160; the auto-start useEffect body at 165-225 that you will extract; the effect dep array at 225; the MOUNT-ONLY cleanup at 228; the top action row `<span className='flex gap-2 w-full items-start'>` at 361 and the sibling share `<button>` at 363-375)
    - .planning/phases/02-on-demand-replay/02-CONTEXT.md (D-01 placement in the top action row; D-02 button sizing/aria; D-03 replay is independent of the seen-flag and must NOT modify tourSeenAtom; D-04 extract to ONE reusable startTour(), destroy existing tourDriverRef.current before starting, reuse the exact Phase-1 driver config, keep the auto-start gate on the effect side, bypass gates on the "?" side, KEEP the mount-only cleanup; D-05 real <button>, English aria-label + title, NOT a tour step)
    - .planning/phases/01-guided-first-run-tour/01-VERIFICATION.md (the `live_browser_verification` block documenting the teardown defect + fix in commit a818626 — the dep-change teardown that silently destroyed the tour and set tourSeen; this MUST NOT regress)
    - .planning/phases/01-guided-first-run-tour/01-UI-SPEC.md (Overlay dimming rgba values by theme; Copywriting Contract doneBtnText/Next/Back; the config surface the extracted startTour must preserve verbatim)
  </read_first>
  <action>
    Modify `src/app/page.tsx` only. Three edits: (A) extract `startTour()`, (B) rewire the auto-start effect to call it, (C) add the "?" button. Add NO fenced code blocks — use the exact identifiers and config keys below. Do NOT change any tour content, any `data-tour` attribute, any step, or `useDragDropTouch`.

    Import: add `QuestionSvg` to the existing named import from `'@/lib/svgs'` at line 24 (currently `import { ShareSvg } from '@/lib/svgs';` → include `QuestionSvg`).

    (A) Extract `startTour()`. Move the tour build+configure+drive logic that is currently inline in the auto-start `useEffect` body (lines ~169-224: the `resolveTourLocale(...)` call, `buildTourSteps(locale)`, the missing-anchor `.filter(...)` + install-fallback `.map(...)` swap, the one-time `isDarkTheme` read, the entire `driver({...})` config object with its `onDoneClick`/`onCloseClick`/`onDestroyed` callbacks, `tourDriverRef.current = driverObj`, and `driverObj.drive()`) into a single reusable function `startTour`. Implement `startTour` as a `useCallback` inside `Home()` (Claude's Discretion granted by D-04 — a `useCallback` keeps `page.tsx` cleanest and gives the "?" button a stable handler). Its dependency list is `[setTourSeen]` only (the only reactive value it closes over; everything else — `tourDriverRef`, `tourStartedRef` — is a ref, and `buildTourSteps`/`resolveTourLocale`/`SUPPORTED_LOCALES`/`TOUR_INSTALL_FALLBACK_DESCRIPTION`/`driver` are module-level).

    Inside `startTour()`, add the D-04 anti-stacking guard as the FIRST statements: destroy any existing instance held in `tourDriverRef.current` and null it out — `tourDriverRef.current?.destroy(); tourDriverRef.current = null;` — BEFORE resolving locale / building steps / calling `driver()`. This guarantees a replay never stacks two overlays or leaks the previous driver. Then run the extracted logic verbatim (locale resolution behind the existing `typeof navigator !== 'undefined'` guard; `buildTourSteps`; the same missing-anchor `document.querySelector` filter that keeps element-less steps; the same install-fallback swap keyed on `document.querySelector('[data-tour="tour-install"] button')` being null; the one-time `document.documentElement.getAttribute('data-theme')` read producing `overlayColor: 'rgba(0,0,0,0.65)'` for dark else `'rgba(0,0,0,0.45)'`; the full `driver({...})` config with `allowClose: true`, `overlayClickBehavior: 'close'`, `allowKeyboardControl: true`, `disableActiveInteraction: true`, `smoothScroll: true`, `stagePadding: 4`, `showProgress: true`, `nextBtnText: 'Next'`, `prevBtnText: 'Back'`, `doneBtnText: "Got it, let's go"`, and the three seen-setting callbacks `onDoneClick`/`onCloseClick`/`onDestroyed` unchanged from Phase 1). End with `tourDriverRef.current = driverObj; driverObj.drive();`. The config MUST be the Phase-1 config unchanged — copy it; do not tweak values.

    Critically for D-03: `startTour()` itself does NOT read or write `tourSeenAtom`. It never calls `setTourSeen` directly — the only `setTourSeen(true)` calls remain inside the three driver callbacks (which fire on Done/close/destroy, harmlessly re-setting an already-true flag on a replay). `startTour()` also does NOT check `tourSeen` or the auto-start gates; gating is the caller's responsibility (the effect gates; the "?" does not).

    (B) Rewire the auto-start effect. Replace the auto-start `useEffect`'s extracted body with a call to `startTour()` while KEEPING its gate: the effect keeps its early return `if (!hydrated || tourSeen || !effectiveAll || tourStartedRef.current) return;`, then `tourStartedRef.current = true;`, then `startTour();`. This preserves TOUR-01 (auto-start once, after hydration + content-ready) and TOUR-02 (never re-fires once `tourSeen` is true) exactly as Phase 1 shipped them. Update the effect's dependency array to include `startTour` alongside `[hydrated, tourSeen, effectiveAll]` (it is now referenced inside the effect); because `startTour` is a `useCallback` depending only on the stable `setTourSeen`, its identity is stable and will not cause extra re-runs. Do NOT move `tourStartedRef.current = true` into `startTour()` — it must stay in the effect so the "?" path (which bypasses that gate) does not flip the auto-start guard.

    (C) KEEP the mount-only cleanup at line 228 EXACTLY as-is: `useEffect(() => () => { tourDriverRef.current?.destroy(); tourDriverRef.current = null; }, []);`. Do NOT add any dependency-change teardown, do NOT add cleanup to the auto-start effect, do NOT return a destroy from the auto-start effect. Reintroducing a dep-change teardown is the exact Phase-1 defect fixed in commit a818626 (documented in 01-VERIFICATION.md `live_browser_verification`): `effectiveAll` changes identity when the network fetch resolves after the cached value, re-running the effect; a cleanup on that effect would destroy the tour milliseconds after it starts and (via `onDestroyed`) silently set `tourSeen`. Because `startTour()` now self-destroys any prior instance up front, a replay is safe without any per-effect teardown.

    Add the "?" replay button (D-01/D-02/D-05) inside the top action row `<span className='flex gap-2 w-full items-start'>` (line 361), as a sibling of the share button — a natural spot is immediately after the share `<button>` (before `<ThemeToggle />`) or right after `<CurrencyListModal .../>`; placement within the span is cosmetic. It is a real `<button type="button">` mirroring the share button: `onClick={() => startTour()}` (or `onClick={startTour}`), an English `aria-label="Replay tour"` and matching `title="Replay tour"` (Claude's Discretion on exact wording per D-05 — "Replay tour" is fine), and the same sizing/hit-area classes as the share button (`className="h-[52px] w-[30px] shrink-0 flex items-center justify-center"`). Render `<QuestionSvg />` as its only child. Per D-05, this button is NOT a tour step: do NOT add a `data-tour` attribute to it, and do NOT add any translation key for its label (30-locale i18n is deferred to Phase 3). The button renders unconditionally in the top row (it is inside the span that always renders in the main return), so it is visible regardless of `tourSeen` (D-01 / success criterion 1).
  </action>
  <verify>
    <automated>grep -q "import { QuestionSvg, ShareSvg } from '@/lib/svgs'" src/app/page.tsx || grep -q 'QuestionSvg' src/app/page.tsx; grep -q 'const startTour' src/app/page.tsx && grep -Eq 'onClick=\{.*startTour' src/app/page.tsx && grep -q 'aria-label="Replay tour"' src/app/page.tsx && grep -q 'tourDriverRef.current?.destroy(); tourDriverRef.current = null;' src/app/page.tsx && grep -q 'disableActiveInteraction: true' src/app/page.tsx && npm run build && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Source (import): `grep -c 'QuestionSvg' src/app/page.tsx` >= 2 (imported from '@/lib/svgs' AND rendered as `<QuestionSvg />` in the button).
    - Source (D-04 single reusable fn): `grep -c 'const startTour' src/app/page.tsx` == 1 (one extracted function); `grep -c 'driver({' src/app/page.tsx` == 1 (the config exists in exactly one place — no duplicated driver config).
    - Source (both callers route through it): `startTour` is called from inside the auto-start effect AND from the button's onClick — `grep -Eq 'onClick=\{.*startTour' src/app/page.tsx` succeeds, and `grep -c 'startTour(' src/app/page.tsx` >= 2 (definition-call sites: effect body + button; count may be higher).
    - Source (D-04 anti-stacking guard): `grep -q 'tourDriverRef.current?.destroy(); tourDriverRef.current = null;' src/app/page.tsx` succeeds AND that statement appears at least twice — once as the first lines of `startTour()` and once in the mount-only cleanup (the mount-only cleanup at the end of the file is preserved).
    - Source (auto-start gate preserved / TOUR-02): the auto-start effect still early-returns on `if (!hydrated || tourSeen || !effectiveAll || tourStartedRef.current) return;` and still sets `tourStartedRef.current = true;` before calling `startTour()` (grep the effect region for all four gate tokens + the ref assignment).
    - Source (D-03 no seen write from replay path): `grep -c 'setTourSeen(true)' src/app/page.tsx` == 3 (only the three driver callbacks onDoneClick/onCloseClick/onDestroyed — unchanged from Phase 1); `startTour()` body contains NO `setTourSeen` call and NO read of `tourSeen` (spot-check: `setTourSeen` appears only inside the `driver({...})` callbacks, not in the button handler or startTour prelude).
    - Source (no regression of the a818626 bug): the auto-start `useEffect` returns NO cleanup function (it does not `return () => ...`); the ONLY destroy-on-cleanup is the standalone mount-only `useEffect(() => () => { ... }, [])` — `grep -c 'useEffect(() => () =>' src/app/page.tsx` == 1.
    - Source (D-01/D-02/D-05 button): `grep -q 'aria-label="Replay tour"' src/app/page.tsx` and `grep -q 'title="Replay tour"' src/app/page.tsx` succeed; the button uses `type="button"` and the share-button sizing `h-[52px] w-[30px]`; it has NO `data-tour` attribute (the button JSX region does not contain `data-tour`); no new translation key was added (`git diff` shows no edits to `src/lib/translations.ts`).
    - Source (config unchanged): `grep -q 'disableActiveInteraction: true' src/app/page.tsx`, `grep -q 'allowClose: true' src/app/page.tsx`, `grep -q "doneBtnText: \"Got it, let's go\"" src/app/page.tsx`, `grep -q 'TOUR_INSTALL_FALLBACK_DESCRIPTION' src/app/page.tsx`, and both `grep -q "rgba(0,0,0,0.65)"` and `grep -q "rgba(0,0,0,0.45)"` succeed (the Phase-1 config survived the extraction verbatim).
    - Behavior/test-command: `npm run build` succeeds AND `npx vitest run` still passes (the existing Phase-1 fns/tourSteps/api tests remain green; this plan adds no failing tests and breaks none).
    - Manual QA (runtime, per 01-VERIFICATION human-verify precedent — driver.js runtime wiring cannot be fully confirmed by static analysis): in `next dev`, (1) with `localStorage.tourSeen='true'` set, load `/` → no auto-start, but the "?" is visible; click "?" → the welcome card appears and the full 8-step tour runs from step 1 with Next/Back/Skip working; (2) rapidly click "?" twice → only ONE overlay is ever shown (no stacked overlays); (3) after replaying and dismissing, `localStorage.getItem('tourSeen')` is still `'true'` and reloading `/` shows no auto-start (TOUR-02 intact); (4) in a fresh browser (localStorage cleared), first load still auto-starts once and never re-fires after being seen (Phase-1 regression check).
  </acceptance_criteria>
  <done>src/app/page.tsx has a single reusable `startTour()` (extracted from the auto-start effect, self-destroying any prior driver instance first), the auto-start effect calls it behind its preserved hydrated/!tourSeen/effectiveAll/tourStartedRef gate, a persistent "?" `<button>` in the top action row calls `startTour()` on click bypassing those gates (never writing tourSeenAtom), the mount-only cleanup is preserved with no dep-change teardown reintroduced, and build + existing tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user click ("?" button) → app | A user-initiated UI event that invokes `startTour()`; no data crosses, no input parsed |
| localStorage → app | The `tourSeen` flag remains user/attacker-editable client state (read by the auto-start effect; NOT touched by the replay path) |
| tour step copy → DOM | driver.js renders Phase-1 static plain-text step titles/descriptions into `document.body` (unchanged this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `tourSeen` localStorage flag | accept | Client-only cosmetic per-browser state (Phase-1 accepted, D-locked). The replay path deliberately does NOT read/write it (D-03), so tampering only affects auto-start, not the "?" replay; worst case is the tour re-shows or not-shows for that one browser. No privilege escalation, no data exposure, no backend. |
| T-02-01-02 | Denial of Service (double overlay / driver leak) | `startTour()` invoked repeatedly via the "?" button | mitigate | `startTour()` destroys and nulls any existing `tourDriverRef.current` as its first action before constructing a new `driver()` (D-04), so rapid re-clicks never stack overlays or leak the previous instance. The mount-only cleanup still destroys the live instance on genuine unmount. |
| T-02-01-03 | Denial of Service (regression: tour destroyed on dep-change) | the auto-start `useEffect` teardown | mitigate | The auto-start effect returns NO cleanup; the sole destroy-on-cleanup is the standalone mount-only `useEffect(..., [])` — preserving the commit-a818626 fix (01-VERIFICATION.md). Acceptance criteria grep-assert exactly one `useEffect(() => () =>` and no `return () =>` on the auto-start effect. |
| T-02-01-04 | Injection / Information Disclosure | `QuestionSvg` icon + `aria-label`/`title` copy + reused tour step copy rendered into the DOM | mitigate | `QuestionSvg` is a static inline `<path>` (no dynamic `d`, no `dangerouslySetInnerHTML`); the button's `aria-label`/`title` are hard-coded English string literals; all tour copy is the Phase-1 static constants with no interpolation of user/URL/navigator input. No injection vector introduced. |
| T-02-01-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs NO packages. `driver.js` was already pinned `^1.6.0` and audited [OK] in Phase-1 RESEARCH (Package Legitimacy Audit). No new dependency, no `[ASSUMED]`/`[SUS]`/`[SLOP]` package, so no blocking human checkpoint is required. |
</threat_model>

<verification>
- `npm run build` succeeds with both `src/lib/svgs.tsx` and `src/app/page.tsx` compiling.
- `npx vitest run` still passes (Phase-1 `fns.test.ts` / `tourSteps.test.ts` / `api.test.ts` / `route.test.ts` remain green; this plan adds no failing tests).
- `grep -c 'const startTour' src/app/page.tsx` == 1 and `grep -c 'driver({' src/app/page.tsx` == 1 (single reusable tour launcher; no duplicated config — D-04).
- `grep -c 'setTourSeen(true)' src/app/page.tsx` == 3 (only the three driver callbacks; the "?" replay path never writes the seen flag — D-03 / TOUR-02 preserved).
- `grep -c 'useEffect(() => () =>' src/app/page.tsx` == 1 and the auto-start effect returns no cleanup (the a818626 dep-change-teardown bug is not reintroduced).
- `grep -q 'aria-label="Replay tour"' src/app/page.tsx` and `grep -q 'QuestionSvg' src/app/page.tsx` succeed (persistent "?" control wired to the new icon — TOUR-03 / success criterion 1).
- Manual QA (dev): with `tourSeen='true'`, "?" relaunches the full 8-step tour from step 1; double-clicking "?" never stacks two overlays; after a replay, `tourSeen` stays `'true'` and auto-start never re-fires (success criteria 2 & 3).
</verification>

<success_criteria>
- A persistent "?" help control is visible in the home page's top action row regardless of `tourSeen` state (Phase 2 success criterion 1 / TOUR-03).
- Clicking "?" launches the identical Phase-1 tour from step 1 (welcome card first), with full forward/back/skip behavior intact (success criterion 2).
- Replaying does not reset or corrupt the seen-flag: the "?" path never writes `tourSeenAtom`; `tourSeen` remains true across a manual replay, and the auto-start effect never re-fires once seen (success criterion 3 / TOUR-02 preserved).
- The tour launch logic lives in ONE reusable `startTour()` called by both the auto-start effect and the "?" button; `startTour()` self-destroys any prior instance so replays never stack overlays; the mount-only cleanup is preserved with no dep-change teardown reintroduced (D-04, no regression of commit a818626).
- `npm run build` and `npx vitest run` pass; only `src/lib/svgs.tsx` and `src/app/page.tsx` are modified.
</success_criteria>

<output>
Create `.planning/phases/02-on-demand-replay/02-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
