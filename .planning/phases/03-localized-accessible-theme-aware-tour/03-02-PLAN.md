---
phase: 03-localized-accessible-theme-aware-tour
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/lib/translations.ts
  - src/lib/tourSteps.test.ts
autonomous: true
requirements: [I18N-01]
user_setup: []

must_haves:
  truths:
    - "All 30 supported locales have a complete `tour` namespace (all 19 keys), so the tour renders in the visitor's active language with no English leakage except intentional fallback (I18N-01, D-06)"
    - "Every translated tour string fits the popover character budgets so no locale overflows/wraps at the mobile popover width (Pitfall 7)"
  artifacts:
    - path: "src/lib/translations.ts"
      provides: "tour namespace on all 30 locale objects (en already done in 03-01; this plan adds the other 29)"
      contains: "tour:"
    - path: "src/lib/tourSteps.test.ts"
      provides: "coverage test asserting all locales have a full tour namespace + character-budget assertions"
      contains: "SUPPORTED_LOCALES"
  key_links:
    - from: "src/lib/translations.ts (29 non-en locales)"
      to: "buildTourSteps(locale) via getTourString"
      via: "translations[locale].tour.<key>"
      pattern: "tour:"
    - from: "src/lib/tourSteps.test.ts"
      to: "translations[locale].tour completeness"
      via: "iterate SUPPORTED_LOCALES, assert every key present"
      pattern: "SUPPORTED_LOCALES"
---

<objective>
Author the `tour` namespace for the 29 non-English locales (D-06) so every one of the 30 supported languages renders the full tour in-language, and lock the coverage + character-budget contract with a test. This is the deliverable that makes I18N-01 real (English fallback is a safety net, not the target).

Purpose: Fulfills I18N-01's "all 30 supported locales" and D-06 ("Claude authors all 30 locales"). Depends on 03-01, which defines the English source strings and the exact key set every locale must mirror.
Output: 29 additional `tour: {...}` blocks in `translations.ts`, tone/length-matched to each locale's existing `home`/`settings` entries and within budget; a Vitest coverage + budget test over `SUPPORTED_LOCALES`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-localized-accessible-theme-aware-tour/03-CONTEXT.md
@.planning/phases/03-localized-accessible-theme-aware-tour/03-PATTERNS.md
@.planning/phases/03-localized-accessible-theme-aware-tour/03-UI-SPEC.md

<interfaces>
<!-- After 03-01, translations.en.tour is the authoritative source. This plan mirrors its key set into 29 locales. -->

The 19-key `tour` shape each locale must contain (from 03-01 / 03-UI-SPEC.md §Copywriting Contract):
welcomeTitle, welcomeBody, step1Title, step1Body, step2Title, step2Body, step3Title, step3Body,
step4Title, step4Body, step5Title, step5Body, step6Title, step6Body, step7Title, step7Body,
step8Title, step8Body, step8FallbackBody, nextBtn, prevBtn, doneBtn, replayLabel.

Locale objects & line anchors in src/lib/translations.ts (from PATTERNS.md — verify current lines, they shift as `tour` blocks are added):
en(2, done in 03-01), 'zh-TW'(22), 'zh-CN'(42), ja(62), ko(82), fr(102), de(122), es(142), it(162),
pt(182), ru(202), 'ar'(222 — RTL), hi(242), bn(262), pa(282), 'ur'(302 — RTL), vi(322), th(342),
id(362), ms(382), nl(402), pl(422), ro(442), sv(462), no(482), da(502), fi(522), sk(542), sl(562), tr(582).

Character budgets (03-UI-SPEC.md §Typography, lines 49-59):
- title ≤ 40 chars (CJK: count glyphs)
- body ≤ 110 chars Latin; ≤ 55 glyphs CJK (zh-TW, zh-CN, ja, ko)
- button label (nextBtn/prevBtn/doneBtn) ≤ 12 chars in ANY locale (tightest budget — 3 buttons + progress must fit one footer row at the mobile width)

Quoting rule (PATTERNS.md:81): keep existing per-locale key quoting exactly — `'zh-TW':`, `'zh-CN':`, `'ar':`, `'ur':` stay quoted; bare keys (`ja:`, `de:`) stay bare. Add the `tour` block as the third sibling after that locale's `settings` block.
NO emoji in `tour.*` values (PATTERNS.md:85) — deliberate deviation from `settings.*`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author `tour` namespace for all 29 non-English locales</name>
  <files>src/lib/translations.ts</files>
  <read_first>
    - src/lib/translations.ts (whole file — read each locale's existing `home`/`settings` to match register/tone/length before authoring that locale's `tour`)
    - 03-UI-SPEC.md §Copywriting Contract (lines 78-105 — English source + authoring rule D-06: match sibling namespace tone, no emoji, no HTML, no concatenation, button labels single short words translatable without reordering)
    - 03-UI-SPEC.md §Typography (lines 49-59 — per-locale character budgets, CJK glyph rule)
    - 03-PATTERNS.md §src/lib/translations.ts (lines 21-88 — 19-key list, camelCase, quoting rule, RTL locale anchors, no-emoji rule)
  </read_first>
  <action>
    For each of the 29 non-English locales (all `SUPPORTED_LOCALES` except `en`, which 03-01 already did), add a `tour: { ... }` object as the third sibling after that locale's `settings` block, containing all 19 keys. Translate each key from the English source (03-01's `en.tour`), matching the tone/register and approximate length of that locale's existing `home`/`settings` copy. camelCase keys, verbatim key names from the interface list. NO emoji. No embedded HTML or string concatenation. Keep every value within the character budgets — for CJK locales (zh-TW, zh-CN, ja, ko) budget by glyph count (title ≤40, body ≤55 glyphs); for all locales button labels ≤12 chars (nextBtn/prevBtn/doneBtn must be single short translatable words — avoid multi-word constructions like "Continue to next step"). For the notoriously verbose locales (de, fi), paraphrase tersely to stay within budget rather than translating literally — native-speaker review is an accepted non-blocking follow-up per D-06. Preserve exact existing per-locale key quoting (quoted `'zh-TW'`/`'ar'`/`'ur'`, bare `ja`/`de`); do not re-quote or unquote any locale key. Do not touch `en.tour`, `home`, or `settings` in any locale.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/moneyrate && npx tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Every locale object contains a `tour:` block: `grep -c "    tour: {" src/lib/translations.ts` === 30 (en + 29). (Indent is 4 spaces, matching `home:`/`settings:` nesting.)
    - `npx tsc --noEmit` passes — TypeScript infers `translations[locale].tour` as structurally compatible with `typeof translations.en.tour` (all 19 keys present in each; a missing key surfaces as a type error only where `getTourString`'s `keyof TourNamespace` is used, but the completeness test in Task 2 is the authoritative gate).
    - No emoji in any `tour.*` value: `grep -nP "tour:" src/lib/translations.ts` context contains no emoji (verified structurally by Task 2's test + manual scan).
    - No new keys or renamed keys — each `tour` block key set matches `en.tour` exactly (Task 2 asserts this).
  </acceptance_criteria>
  <done>All 30 locales have a complete `tour` namespace with all 19 keys, tone/length-matched, no emoji, within budget; tsc clean; existing `home`/`settings`/`en.tour` untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage + character-budget test over all locales</name>
  <files>src/lib/tourSteps.test.ts</files>
  <read_first>
    - src/lib/tourSteps.test.ts (whole file — existing describe/it style; SUPPORTED_LOCALES already imported per PATTERNS.md:443)
    - src/lib/tourSteps.ts:11 (SUPPORTED_LOCALES export)
    - 03-UI-SPEC.md §Typography (lines 49-59 — budgets to encode as assertions)
    - 03-RESEARCH.md §Common Pitfalls 7 (character-budget test recommended as phase coverage, not manual review of 630 strings)
    - 03-PATTERNS.md §Testing Analog 3 (lines 442-443 — length-check test style)
  </read_first>
  <behavior>
    - For every locale in SUPPORTED_LOCALES, `translations[locale].tour` exists and contains exactly the same 19 keys as `translations.en.tour` (no missing, no extra) → completeness.
    - For every locale, each title key value length ≤ 40 (CJK locales checked against a CJK-aware count, or the same ≤40 which is stricter-safe); each body key ≤ 110 Latin / ≤ 55 for the 4 CJK locales; each of nextBtn/prevBtn/doneBtn ≤ 12.
    - No `tour.*` value contains an emoji (a simple regex over the astral/emoji ranges, or assert none of a small emoji-charset appears).
  </behavior>
  <action>
    Extend `src/lib/tourSteps.test.ts` with a `describe('tour namespace coverage', ...)` block. Import `translations` from `@/lib/translations` and `SUPPORTED_LOCALES` from `@/lib/tourSteps` (SUPPORTED_LOCALES is already imported per PATTERNS.md:443 — reuse it). Derive `const TOUR_KEYS = Object.keys(translations.en.tour)` as the canonical 19-key set. Iterate `SUPPORTED_LOCALES` with `it.each` (or a plain `for...of` inside one `it`) asserting: (a) `translations[locale].tour` is defined; (b) `Object.keys(translations[locale].tour).sort()` deep-equals `TOUR_KEYS.sort()`; (c) title-key lengths ≤ 40, body-key lengths ≤ 110 (≤ 55 for `['zh-TW','zh-CN','ja','ko']`), button-key lengths ≤ 12; (d) no value matches an emoji regex (e.g. `/\p{Extended_Pictographic}/u`). Plain node env (no jsdom). Do not modify the existing tests.
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/moneyrate && npx vitest run src/lib/tourSteps.test.ts 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tour namespace coverage" src/lib/tourSteps.test.ts` === 1.
    - The test asserts all `SUPPORTED_LOCALES` have the full 19-key set (completeness) — a locale with a missing/extra key FAILS the run.
    - The test asserts button labels ≤ 12 and titles ≤ 40 and bodies within the Latin/CJK budget — an over-budget string FAILS the run.
    - `npx vitest run src/lib/tourSteps.test.ts` passes (proves Task 1 authored all 30 completely and within budget).
  </acceptance_criteria>
  <done>A single coverage test enforces all-30-locale `tour` completeness, the character budgets, and no-emoji; it passes, proving Task 1's authoring is complete and in-budget; existing tourSteps tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| translations.ts static strings → driver.js popover | 29 additional author-generated locale strings rendered into the popover |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Tampering/EoP | 29 new locale tour strings rendered into `.driver-popover` | accept | driver.js sets title/description via `innerText` not `innerHTML` (RESEARCH.md §Security Domain); all strings are static author content (D-06), plain text, no embedded HTML (Copywriting Contract forbids it) — the coverage test additionally rejects emoji but does not need HTML-injection defense since the render path is text-only. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH.md §Package Legitimacy Audit: N/A). No install step. |
</threat_model>

<verification>
- `npx vitest run src/lib/tourSteps.test.ts` — coverage + budget test green across all 30 locales.
- `npx tsc --noEmit` — no type errors from the added locale blocks.
- Spot-check (documented, not a gate here): `buildTourSteps('ar')[1].popover.title` returns the Arabic step-1 title, `buildTourSteps('de')` button labels fit. Full RTL visual QA is 03-04.
</verification>

<success_criteria>
- I18N-01 content complete: all 30 supported locales render the full tour in-language; English is only a per-string fallback for a genuinely-missing key (D-04/D-06).
- Character budgets enforced by an automated test so no locale overflows the mobile popover (Pitfall 7 closed).
- No emoji, no HTML, no concatenation in tour copy.
</success_criteria>

<output>
Create `.planning/phases/03-localized-accessible-theme-aware-tour/03-02-SUMMARY.md` when done.
</output>
