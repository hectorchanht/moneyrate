---
phase: 03-localized-accessible-theme-aware-tour
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - e2e/tour.spec.ts
  - src/theme/tour.css
  - src/app/page.tsx
autonomous: false
requirements: [A11Y-01]
user_setup: []

must_haves:
  truths:
    - "A keyboard-only user can advance, go back, and dismiss the entire tour with visible focus rings on every focusable control (A11Y-01, D-07 item 1)"
    - "After the tour closes/completes via keyboard, focus returns to the '?' replay button so the keyboard user keeps their place (D-07 item 2)"
    - "When prefers-reduced-motion is set, the tour does not animate/smooth-scroll (D-07 item 3, clears Phase-2 deferred item)"
    - "The tour popover follows the active light/dark theme with no wrong-theme flash (SC3 — verify-only, already implemented in tour.css)"
  artifacts:
    - path: "e2e/tour.spec.ts"
      provides: "first tour e2e coverage: keyboard drive + focus-restore assertion (verify-first) + reduced-motion"
      contains: "keyboard"
    - path: "src/theme/tour.css"
      provides: ":focus-visible rings on 5 elements + prefers-reduced-motion block"
      contains: ":focus-visible"
    - path: "src/app/page.tsx"
      provides: "reduced-motion read wired into driver config (animate/smoothScroll); .tour-replay-btn class; focus-restore ONLY if verify test proves built-in insufficient"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "src/theme/tour.css"
      to: "driver.js footer buttons + .tour-replay-btn"
      via: ":focus-visible outline oklch(var(--p))"
      pattern: "focus-visible"
    - from: "src/app/page.tsx startTour()"
      to: "driver({ animate, smoothScroll })"
      via: "window.matchMedia('(prefers-reduced-motion: reduce)').matches read once"
      pattern: "prefers-reduced-motion"
    - from: "e2e/tour.spec.ts"
      to: "'?' replay button focus after Esc/Done"
      via: "keyboard Tab+Enter then Escape, assert document.activeElement"
      pattern: "activeElement"
---

<objective>
Make the tour fully keyboard-operable with visible focus states, restore focus on exit, and honor prefers-reduced-motion — the A11Y-01 slice. Critically, this plan is VERIFY-FIRST on focus restoration: driver.js 1.6.0 already captures/restores `document.activeElement` natively (RESEARCH finding #1), so the plan writes a keyboard-driven Playwright test to prove whether any restore code is needed BEFORE writing it, avoiding a redundant double-restore.

Purpose: Satisfies A11Y-01 (D-07 all three items) and documents SC3 theme compliance. Depends on 03-01 so the replay button already carries its i18n label and startTour reads languageAtom (avoids a merge conflict on the same page.tsx regions). Wave 2 — runs in PARALLEL with 03-02: 03-02 touches only translations.ts + tourSteps.test.ts, while this plan touches e2e/tour.spec.ts + tour.css + page.tsx (disjoint file sets, neither depends on the other).
Output: `e2e/tour.spec.ts` (first tour e2e), `:focus-visible` + reduced-motion CSS in `tour.css`, reduced-motion wiring + `.tour-replay-btn` class in `page.tsx`, and focus-restore code ONLY if the test proves the built-in insufficient.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-localized-accessible-theme-aware-tour/03-CONTEXT.md
@.planning/phases/03-localized-accessible-theme-aware-tour/03-PATTERNS.md
@.planning/phases/03-localized-accessible-theme-aware-tour/03-RESEARCH.md
@.planning/phases/03-localized-accessible-theme-aware-tour/03-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs — extracted from codebase + research. -->

driver.js real button classes (confirmed tour.css:44-71, RESEARCH.md): `.driver-popover-next-btn`, `.driver-popover-prev-btn`, `.driver-popover-done-btn`, `.driver-popover-close-btn`, `.driver-popover-footer-btn`. NEVER select by localized aria-label/title (CLAUDE.md anchor rule) — use class names.

Existing (src/app/page.tsx:191): one-time theme read inside startTour — `const isDarkTheme = document.documentElement.getAttribute('data-theme') !== 'light';` — copy this "read once per drive() call" shape for the reduced-motion read.
Existing driver config (src/app/page.tsx:193-217): `smoothScroll: true` (:199, make conditional), `animate` NOT set (driver.js default true — set it conditional), exit paths `onDoneClick`/`onCloseClick`/`onDestroyed` (:206-216) already funnel through driverObj.
Existing (src/app/page.tsx:387-395): the "?" replay button — add a stable `.tour-replay-btn` class to its existing className (after 03-01, its label is already i18n).

driver.js built-in focus behavior (RESEARCH.md §Common Pitfalls 3, Open Question 1): captures `document.activeElement` at drive()-time, restores on every exit. VERY LIKELY satisfies D-07 item 2 with zero code. Open question: keyboard-triggered replay ("?" focused via Tab+Enter) — does the built-in restore land back on "?".

CSS to add (03-UI-SPEC.md §Keyboard Accessibility Contract, exact rules at lines 134-142 + 159-164):
```
.driver-popover-next-btn:focus-visible, .driver-popover-prev-btn:focus-visible,
.driver-popover-done-btn:focus-visible, .driver-popover-close-btn:focus-visible {
  outline: 2px solid oklch(var(--p)); outline-offset: 2px;
}
.tour-replay-btn:focus-visible { outline: 2px solid oklch(var(--p)); outline-offset: 2px; }
@media (prefers-reduced-motion: reduce) { .driver-popover { --driver-animation-duration: 0ms; } }
```

Playwright seeding (e2e/home.spec.ts, PATTERNS.md:460-483): `page.addInitScript` sets localStorage BEFORE `page.goto('/')`; `mockRates(page)` mocks the rate API. Reuse the same helper shape. Drive the "?" via keyboard (Tab + Enter), NOT `page.click()`, per RESEARCH Open Question 1.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: VERIFY-FIRST — keyboard-driven Playwright baseline for focus restoration</name>
  <files>e2e/tour.spec.ts</files>
  <read_first>
    - e2e/home.spec.ts (whole file — seed()/mockRates pattern, localStorage seeding via addInitScript, theme-toggle test as the closest analog)
    - 03-RESEARCH.md §Common Pitfalls 3 (verify-first mandate) and §Open Questions 1 (drive via keyboard Tab+Enter, not page.click)
    - 03-PATTERNS.md §Testing / Playwright e2e (lines 458-483 — new e2e/tour.spec.ts shape)
    - src/app/page.tsx:387-395 (the "?" button — its DOM handle for the test; will gain .tour-replay-btn in Task 3)
  </read_first>
  <what-built>
    A new `e2e/tour.spec.ts` that seeds localStorage (omit `tourSeen` OR set it, plus set `language` if testing a locale), navigates to `/`, and drives the tour via the KEYBOARD: Tab to the "?" replay button, Enter to open the tour, then exercises the keyboard path — ArrowRight/ArrowLeft to step, Escape to close — and asserts `document.activeElement` after Escape (and after Done) is the "?" replay button. The test targets the "?" button by a stable hook (its `.tour-replay-btn` class added in Task 3, or a data attribute), NEVER by localized aria-label. This test runs FIRST to establish whether driver.js 1.6.0's built-in restore already returns focus to "?" on the keyboard-triggered path — before any restore code is written in Task 3.
  </what-built>
  <how-to-verify>
    1. Run `cd /Users/laichan/code/tung/moneyrate && npx playwright test e2e/tour.spec.ts` (executor: add `--project=chromium` if the config defines projects).
    2. Observe whether the focus-restoration assertion PASSES against the CURRENT page.tsx (pre-focus-restore-code) — this is the baseline.
    3. Report which case holds:
       - PASS → driver.js built-in already satisfies D-07 item 2; Task 3 adds NO focus-restore code (documents the built-in as sufficient).
       - FAIL → report exactly where focus lands after Escape/Done (e.g. document.body vs a driver button) so Task 3 adds the minimal single restore closure.
    4. Also confirm the keyboard nav itself works: after Enter opens the tour, ArrowRight advances, ArrowLeft goes back, Escape closes (proves allowKeyboardControl, no new code needed).
  </how-to-verify>
  <resume-signal>Report "baseline PASS" (built-in restore sufficient) or "baseline FAIL: focus lands on <X>" — then Task 3 branches accordingly. Type "approved" with the result to continue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add :focus-visible rings + prefers-reduced-motion block to tour.css</name>
  <files>src/theme/tour.css</files>
  <read_first>
    - src/theme/tour.css (whole file — existing selector-grouping style, button-color rules :51-60, spotlight ring token :93, header comment :1-9 documenting --driver-animation-duration)
    - 03-UI-SPEC.md §Keyboard Accessibility Contract (lines 121-167 — exact focus-ring + reduced-motion CSS, width/offset/color rationale)
    - 03-PATTERNS.md §src/theme/tour.css (lines 299-363 — where each rule slots in, matching existing comma-selector convention)
  </read_first>
  <action>
    In `src/theme/tour.css`, add the grouped `:focus-visible` rule for the four driver button classes (`.driver-popover-next-btn`, `.driver-popover-prev-btn`, `.driver-popover-done-btn`, `.driver-popover-close-btn`) with `outline: 2px solid oklch(var(--p)); outline-offset: 2px;` — matching the exact values in 03-UI-SPEC.md §Keyboard Accessibility Contract. Use `:focus-visible` only, never bare `:focus`. Add a separate `.tour-replay-btn:focus-visible` rule with the same outline (grouped under a comment noting it targets the app's replay control). Use the same `oklch(var(--p))` accent token already used for the spotlight ring at :93 — introduce no new hue. Add the `@media (prefers-reduced-motion: reduce) { .driver-popover { --driver-animation-duration: 0ms; } }` block (the file's only exposed custom property, header comment :2-3). Do NOT touch the responsive-width line (:16 — that is 03-04's) or the arrow rules (:73-87).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/moneyrate && grep -c "focus-visible" src/theme/tour.css && grep -c "prefers-reduced-motion" src/theme/tour.css && grep -v '^\s*/\*' src/theme/tour.css | grep -c "outline: 2px solid oklch(var(--p))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "focus-visible" src/theme/tour.css` >= 5 (4 driver buttons in one grouped selector = 4 occurrences + 1 replay = 5).
    - `grep -c ":focus\b" src/theme/tour.css` counts only `:focus-visible` (no bare `:focus` added) — verify no new bare `:focus` selector: `grep -nE ':focus([^-]|$)' src/theme/tour.css` returns nothing new for the tour buttons.
    - tour.css contains `outline: 2px solid oklch(var(--p))` and `outline-offset: 2px` (exact values).
    - tour.css contains `@media (prefers-reduced-motion: reduce)` with `--driver-animation-duration: 0ms`.
    - `.tour-replay-btn:focus-visible` rule present.
    - Line 16 `max-width: 320px` UNCHANGED (03-04 owns it).
  </acceptance_criteria>
  <done>Five elements gain a theme-aware :focus-visible ring using the accent token; a prefers-reduced-motion block zeroes the popover animation duration; no bare :focus selectors, no new hue, responsive-width line untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Wire reduced-motion into driver config, add .tour-replay-btn class, and focus-restore ONLY if Task 1 failed</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx:167-227 (startTour body — the :191 one-time theme read is the pattern for the reduced-motion read; :199 smoothScroll; :206-216 exit paths)
    - src/app/page.tsx:387-395 (the "?" replay button — add .tour-replay-btn to its className)
    - 03-UI-SPEC.md §Keyboard Accessibility Contract items 2 & 3 (lines 145-167 — focus-restore contract + reduced-motion JS read)
    - 03-RESEARCH.md §Code Examples 2 (reduced-motion JS+CSS two-part contract) and §Common Pitfalls 3 (do not add redundant restore if Task 1 passed)
    - 03-PATTERNS.md §src/app/page.tsx items 3, 4, 5 (lines 247-287 — reduced-motion read, focus-restore hook-up, replay button class)
    - The Task 1 baseline result (PASS = no restore code; FAIL = single restore closure)
  </read_first>
  <action>
    In `src/app/page.tsx` `startTour()`: add a one-time read `const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;` beside the existing `isDarkTheme` read (:191, same "read once per drive() call, do not subscribe" shape). In the driver config object, set `animate: !prefersReducedMotion,` (currently unset → driver default true) and change `smoothScroll: true` (:199) to `smoothScroll: !prefersReducedMotion,`. Do NOT touch the teardown/ref model.

    Add the stable class `tour-replay-btn` to the "?" replay button's existing className string (:392) — `className="tour-replay-btn h-[52px] w-[30px] shrink-0 flex items-center justify-center"`. This is the CSS hook Task 2's `:focus-visible` rule targets. Do not select by aria-label.

    Focus restoration — BRANCH on Task 1's baseline:
    - If Task 1 reported baseline PASS: add NO focus-restore code. Add a code comment near the exit callbacks noting driver.js 1.6.0's built-in `document.activeElement` capture/restore satisfies D-07 item 2 (verified in e2e/tour.spec.ts) — cite the test.
    - If Task 1 reported baseline FAIL: add ONE shared restore mechanism. Capture the trigger element once before `driverObj.drive()` — prefer a ref to the "?" replay button (more reliable than `document.activeElement`, which the focus trap may have moved) or fall back to the "?" button as the default landing point per the UI-SPEC. Add a single `restoreFocus()` closure called from all three exit paths (`onDoneClick`/`onCloseClick`/`onDestroyed`) — NOT three ad-hoc capture points. Keep it idempotent (a second call is a no-op).
    Whichever branch, do not reimplement driver.js keyboard nav or the Tab focus trap (D-07 forbids it).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/moneyrate && grep -c "prefers-reduced-motion" src/app/page.tsx && grep -c "tour-replay-btn" src/app/page.tsx && npx tsc --noEmit 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "prefers-reduced-motion" src/app/page.tsx` === 1 (the matchMedia read).
    - `grep -E "animate:|smoothScroll:" src/app/page.tsx | grep -c "!prefersReducedMotion"` === 2 (both conditional).
    - `grep -c "tour-replay-btn" src/app/page.tsx` === 1 (class on the "?" button).
    - Focus-restore branch matches Task 1: if baseline PASS → `grep -c "restoreFocus" src/app/page.tsx` === 0 (no redundant code) + a comment citing the e2e test; if baseline FAIL → exactly one `restoreFocus` closure referenced by all three exit callbacks (`grep -c "restoreFocus()" src/app/page.tsx` === 3 call sites + 1 def, or equivalent single-closure pattern).
    - `npx tsc --noEmit` passes; driver teardown model (ref-held instance, :239 unmount cleanup) unchanged.
  </acceptance_criteria>
  <done>Reduced-motion disables animate+smoothScroll via a single one-time media-query read; the "?" button carries .tour-replay-btn for the focus ring; focus restoration is present iff the Task-1 baseline proved the built-in insufficient (no redundant closure otherwise); no keyboard-nav/focus-trap reimplementation; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: Finalize e2e/tour.spec.ts — reduced-motion + focus-ring presence + green run</name>
  <files>e2e/tour.spec.ts</files>
  <read_first>
    - e2e/tour.spec.ts (the file created in Task 1)
    - e2e/home.spec.ts (seed/mockRates helpers to reuse)
    - 03-UI-SPEC.md §Keyboard Accessibility Contract (all three D-07 items, to cover)
    - 03-RESEARCH.md §Validation Architecture (Playwright is required for :focus-visible / prefers-reduced-motion / real Tab — jsdom cannot)
  </read_first>
  <action>
    Extend `e2e/tour.spec.ts` so it covers all three A11Y-01 items end-to-end in a real browser: (1) keyboard operability — Tab to "?", Enter opens, ArrowRight/ArrowLeft step, Escape closes (assert popover gone); (2) focus restoration — after Escape/Done the "?" button is `document.activeElement` (matching the branch taken in Task 3); (3) reduced-motion — use Playwright's `page.emulateMedia({ reducedMotion: 'reduce' })` before opening the tour and assert the popover renders without the animate transition (assert `--driver-animation-duration` computed to `0ms` on `.driver-popover`, or that `.driver-popover` lacks the fade class). Target the "?" button via its `.tour-replay-btn` class, never a localized aria-label. Reuse `home.spec.ts`'s `mockRates`/seed pattern. Keep the test deterministic (seed rates so the tour anchors render).
  </action>
  <verify>
    <automated>cd /Users/laichan/code/tung/moneyrate && npx playwright test e2e/tour.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/tour.spec.ts` exercises keyboard open (Tab+Enter), step (Arrow keys), close (Escape) — all via `page.keyboard`, no `page.click()` on the "?" button.
    - Asserts `document.activeElement` is the `.tour-replay-btn` after Escape and after Done.
    - Asserts reduced-motion behavior via `page.emulateMedia({ reducedMotion: 'reduce' })`.
    - `npx playwright test e2e/tour.spec.ts` passes.
    - No selector in the spec references a translated aria-label/title.
  </acceptance_criteria>
  <done>e2e/tour.spec.ts fully covers A11Y-01's three items (keyboard nav, focus restore matching Task 3's branch, reduced-motion) in a real browser and passes; all selectors are class/data-based, none localized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS/DOM focus + media-query → browser | Client-side a11y mechanisms only; no data crosses a trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | (n/a — a11y CSS/DOM only) | :focus-visible rings, reduced-motion, focus restoration | accept | Pure client-side CSS + one-time media-query read + optional `.focus()` call. No untrusted input, no injection surface, no persistence. Selectors use driver.js class names / a stable class, never a translated string, so no locale-driven selector injection. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH.md §Package Legitimacy Audit: N/A). No install step. |
</threat_model>

<verification>
- `npx playwright test e2e/tour.spec.ts` — keyboard nav, focus restore, reduced-motion all green.
- `npx tsc --noEmit` — clean.
- Human checkpoint (Task 1) records the focus-restore baseline result, which gates Task 3's branch.
- SC3 theme compliance is verify-only (tour.css already token-driven per UI-SPEC §Theme mechanism); document in the SUMMARY that no theme flash occurs on light/dark toggle — no new code.
</verification>

<success_criteria>
- A11Y-01 met: entire tour operable by keyboard with visible :focus-visible rings on all 5 controls; focus returns to "?" on exit (built-in or minimal closure per the baseline); prefers-reduced-motion disables animation + smooth-scroll.
- SC3 documented as satisfied (theme-following, no code change).
- First tour e2e coverage exists and is deterministic.
</success_criteria>

<output>
Create `.planning/phases/03-localized-accessible-theme-aware-tour/03-03-SUMMARY.md` when done.
</output>
